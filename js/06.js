// Objetos mmath

let resultado;

resultado=Math.PI;
resultado=Math.round(2.5);
resultado=Math.ceil(2.1); //ceil redonde hacia arriba
resultado=Math.floor(2.9); //redondea hacia arriba
resultado=Math.sqrt(144);
resultado=Math.abs(-200);
resultado=Math.min(1,10,3,6,2,.7,99);
resultado=Math.max(1,10,3,6,2,.7,99);
resultado=Math.random();//solo numeros de 0. algo
resultado=Math.floor(Math.random()*30); //genera numeros mayores a 0

console.log(resultado);